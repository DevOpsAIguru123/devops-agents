sonar.projectKey=agentic-devops-portfolio
sonar.projectName=Agentic DevOps Portfolio
sonar.projectVersion=1.0
sonar.sources=.
sonar.sourceEncoding=UTF-8
# Deliberately vulnerable fixtures are exercised by the Trivy blocking test,
# not treated as production code by SonarQube Cloud.
sonar.exclusions=reports/**,.scannerwork/**,.ci-config-scan/**,Dockerfile.vulnerable,deployment.vulnerable.yaml
